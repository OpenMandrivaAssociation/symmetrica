--- a/src/def.h
+++ b/src/def.h
@@ -548,6 +548,9 @@
 INT SYMMETRICA_bricknumber(OP umriss, OP cont, OP res);
 INT SYM_sort(OP a);
 INT SYM_sum(OP a, OP res);
+/* Historical OMV API names (namespace-safe rename of libc-colliding sort/sum) */
+#define sym_sort SYM_sort
+#define sym_sum SYM_sum
 INT S_a_rofword(OP w, OP a, OP r);
 INT S_rofword(OP w, OP r);
 INT Sinus_eingabe(REIHE_zeiger root, INT anzahl);
--- a/src/nu.c
+++ b/src/nu.c
@@ -601,8 +601,8 @@
 INT SYM_sum(OP a, OP res)
 {
     INT erg = OK;
-    COP("sum(1)",a);
-    COP("sum(2)",res);
+    COP("SYM_sum(1)",a);
+    COP("SYM_sum(2)",res);
     CE2(a,res,SYM_sum);
 
     switch(S_O_K(a))
@@ -631,7 +631,7 @@
         break;
 #endif /* MATRIXTRUE */
     default:
-        WTO("sum",a); break;
+        WTO("SYM_sum",a); break;
     };
 
     ENDR("SYM_sum");
