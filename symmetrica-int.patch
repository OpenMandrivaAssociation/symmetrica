--- a/src/def.h
+++ b/src/def.h
@@ -1,5 +1,5 @@
 /* file def.h SYMMETRICA */
-/* INT should always be 4 byte */
+/* INT should always be a 4-byte type (int32_t); use PRIINT/SCNINT in format strings */
 #ifndef DEF_H
 
 /* POSIX says that inttypes.h includes stdint.h */
--- a/src/nb.c
+++ b/src/nb.c
@@ -518,14 +518,14 @@
 
     FILE    *f;
     if ( (f = fopen("PRIMES.DAT","r")) == NULL ||
-        fscanf(f," %ld",&no_primes) == 0 || no_primes < 1L ||
+        fscanf(f," %" SCNINT ,&no_primes) == 0 || no_primes < 1L ||
         (prime_table = (INT *)SYM_calloc((int)no_primes,sizeof(INT))) == NULL )
     {
         no_primes    = 0L;
         return(ERROR);
     }
     for (i=0L;i<no_primes;i++)
-        if (fscanf(f," %ld",&prime_table[i]) != 1)
+        if (fscanf(f," %" SCNINT ,&prime_table[i]) != 1)
         {
             SYM_free(prime_table);
             no_primes    = 0L;
